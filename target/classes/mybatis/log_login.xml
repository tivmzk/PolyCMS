<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="log_login">
	<insert id="login">
		INSERT INTO log_login(member_id, result, reg_date)
		VALUES(#{member.id}, #{result}, SYSDATE)
	</insert>
</mapper>